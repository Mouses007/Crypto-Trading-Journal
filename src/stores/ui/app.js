/**
 * UI: General app state (user, limits, pagination, modals, misc).
 */
export {
    currentUser,
    timeZones,
    queryLimit,
    queryLimitExistingTrades,
    endOfList,
    noData,
    stepper,
    hasData,
    itemToEditId,
    currentDate,
    quill,
    saveButton,
    windowIsScrolled,
    countdownInterval,
    countdownSeconds,
    dailyChartZoom,
    legacy,
    modalDailyTradeOpen,
    selectedItem,
    dailyPagination,
    dailyQueryLimit,
    screenshotsPagination,
    screenshotsQueryLimit,
} from '../globals.js'
