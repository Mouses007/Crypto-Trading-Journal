/**
 * UI: Screenshot viewer/editor state (expand, resize, selection).
 */
export {
    markerAreaOpen,
    editingScreenshot,
    expandedId,
    expandedSource,
    selectedScreenshotIndex,
    selectedScreenshotSource,
    dateScreenshotEdited,
    resizeCompressImg,
    resizeCompressMaxHeight,
    resizeCompressMaxWidth,
    resizeCompressQuality,
    expandedScreenshot,
} from '../globals.js'
