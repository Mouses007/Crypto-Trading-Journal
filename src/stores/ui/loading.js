/**
 * UI: Loading & Mounting state (spinners, chart mount flags).
 * Re-exports from globals — single source of truth remains globals.js.
 */
export {
    spinnerLoadingPage,
    spinnerLoadingPageText,
    renderData,
    spinnerLoadMore,
    tabGettingScreenshots,
    dashboardChartsMounted,
    dashboardIdMounted,
    auswertungMounted,
    barChartNegativeTagGroups,
    renderingCharts,
    spinnerSetups,
    spinnerSetupsText,
} from '../globals.js'
