/**
 * Filters: Option lists and ranges (periodRange, positions, timeFrames, ratios, etc.).
 */
export {
    selectedRange,
    periodRange,
    positions,
    timeFrames,
    ratios,
    grossNet,
    plSatisfaction,
    allTradeTimeframes,
    filteredSuggestions,
    daysBack,
    daysMargin,
    getMore,
    uploadMfePrices,
    brokers,
} from '../globals.js'
