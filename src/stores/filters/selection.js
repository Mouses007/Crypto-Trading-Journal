/**
 * Filters: Selected values (tags, positions, timeframe, ratio, accounts, etc.).
 */
export {
    selectedTags,
    selectedPositions,
    selectedTimeFrame,
    selectedRatio,
    selectedAccount,
    selectedAccounts,
    selectedGrossNet,
    selectedPlSatisfaction,
    selectedBroker,
    selectedDateRange,
    selectedMonth,
    selectedPeriodRange,
    selectedDashTab,
    amountCase,
    amountCapital,
    selectedTradeTimeframes,
    selectedTagIndex,
    tempSelectedPlSatisfaction,
} from '../globals.js'
