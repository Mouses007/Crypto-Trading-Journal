/**
 * Settings: AI report state (generating, error, last saved, label, count).
 */
export {
    aiReportGenerating,
    aiReportError,
    aiReportLastSavedId,
    aiReportLabel,
    aiReportCountBefore,
} from '../globals.js'
