# ── Crypto Trading Journal — Docker-Konfiguration ──
# Kopiere diese Datei nach .env und passe die Werte an:
#   cp .env.example .env

# Port auf dem Host (Standard: 8080)
CTJ_PORT=8080

# ── Verschlüsselung ──
# Fester Schlüssel für API-Key-Verschlüsselung (gleich auf allen Instanzen setzen)
# Generieren mit: openssl rand -hex 32
CTJ_SECRET=

# ── Datenbank ──
# "postgresql" = Externe PostgreSQL (z.B. NAS)
# Zeile entfernen oder leer lassen = SQLite (lokal im Container)
DB_TYPE=postgresql
DB_HOST=192.168.178.100
DB_PORT=5433
DB_USER=tradejournal
DB_PASSWORD=dein_passwort_hier
DB_NAME=tradejournal
